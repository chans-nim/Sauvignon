# Snapshot 검증 및 갭/Short 종목 정리

## 2026-03-17 작업 요약

### 1. 실패 6건 재시도
- **당시 실패 원인**: `meta.duckdb` 파일 잠금 (다른 프로세스가 DB 사용 중).
- **조치**: `src/storage/meta_store.py`에 `connect()` 호출 시 잠금 메시지 감지 시 최대 5회 재시도(지수 백오프) 로직 추가.
- **재실행 결과**: 갭 채우기 재실행 시 **남은 갭은 2건**만 존재 (036220, 198940). 기존 6건(228800, 241590, 261220, 261260, 295000, 414270)은 이미 silver에 반영되어 있어 갭 목록에서 제외됨.

### 2. 갭 채우기 재실행 (2026-03-17)
- 구간: 2016-01-01 ~ 2025-12-31, `coverage_threshold=0.95`
- 결과: **2 intervals** (036220: 2017~2024, 198940: 2017~2022) 수집 완료. 실패 0건.
- 036220: 수집 성공 but **196 rows** (API가 해당 구간에서 더 반환하지 않음).
- 198940: 수집 성공 but **34 rows** (동일).

### 3. 036220, 198940 원인 분석

| 항목 | 036220 (지엔씨에너지) | 198940 (한도이미텍) |
|------|------------------------|---------------------|
| 시장 | KOSDAQ | KOSDAQ |
| Silver 연도별 | 2016: 83(SHORT), 2017~2023: 0, 2024: 196(SHORT), 2025: 234(ok) | 2016: 233(ok), 2017: 34(SHORT), 2018~2022: 0, 2023~2025: ok |
| last_success_date | 2024-12-30 | 2017-02-20 |
| **원인** | KIS API가 2017~2023 구간 데이터를 **제공하지 않음** (상장/거래정지/데이터 미제공 등) | 2018~2022 구간 **API 미제공** (거래정지·상장폐지 후 재상장 등 추정) |

- 수집 실패가 아니라 **API에서 해당 기간 데이터를 내려주지 않아** short/missing으로 남는 케이스.
- 유니버스에는 활성 종목으로 있으나, 과거 구간은 데이터 소스 한계로 보완 불가.

### 4. 최종 스냅샷
- **태그**: `data-snapshot-20260317-1338`
- **행 수**: 6,964,600
- **검증**: 중복 0, invalid price/volume 0, active_symbols 4235 = snapshot_symbols, 2016/2025 missing 0.
- **Short symbol-years**: 14건 (위 2종목 한정). 나머지 종목은 기준 충족.

### 5. 수정된 파일
- `src/storage/meta_store.py`: DB 연결 잠금 시 재시도 추가.
- `scripts/validate_snapshot.py`: `missing_or_short_symbol_years` 쿼리에서 컬럼 ambiguous 해결 (active CTE `USING(symbol)` → `ON u.symbol = f.symbol`, ORDER BY `a.symbol` 명시).
